# Data

This directory contains generated numerical outputs used in the report.

- `generated/`: CSV/JSON outputs used by the figures.
- `paper_tables/`: CSV versions of the numerical tables in the report.
- `manifest.csv`: mapping from each reported figure or table to the
  corresponding data file and script.

The files are deterministic outputs from the scripts in `../code`.
